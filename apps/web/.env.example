# Always use a full URL with scheme. On Vercel/Railway use https://your-api.up.railway.app (no trailing slash).
NEXT_PUBLIC_API_URL="http://localhost:4000"
NEXT_PUBLIC_SOCKET_URL="http://localhost:4000"
