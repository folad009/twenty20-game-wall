DATABASE_URL="postgresql://qa:qa@localhost:5432/qa?schema=public"
JWT_SECRET="change-me-to-a-long-random-string"
JWT_EXPIRES_SEC="604800"
PORT=4000
# Comma-separated, no trailing slashes. Must match the browser origin (e.g. https://your-app.vercel.app)
WEB_ORIGINS="http://localhost:3000"
# Set to 1 to allow any https://*.vercel.app (useful for preview URLs; stricter: list each preview in WEB_ORIGINS)
# CORS_ALLOW_VERCEL=0
ADMIN_PHONE="+15550000001"
ADMIN_NAME="Demo Admin"